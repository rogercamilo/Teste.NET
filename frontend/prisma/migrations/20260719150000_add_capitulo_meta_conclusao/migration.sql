-- Meta de conclusão da leitura por capítulo (YYYY-MM-DD textual). Nula: capítulos
-- existentes seguem sem meta. Define a cadência da leitura do livro.
ALTER TABLE "CapituloLeitura" ADD COLUMN "metaConclusao" TEXT;
